rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow read access to all files by default
    match /{allPaths=**} {
      allow read: if true;
      // Temporarily allow write for any authenticated user to debug upload issues
      allow write: if request.auth != null;
    }
  }
}
