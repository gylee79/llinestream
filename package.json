{
  "name": "llinestream-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "src",
    "functions"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=src",
    "build": "npm run build --workspace=src",
    "start": "npm run start --workspace=src",
    "lint": "npm run lint --workspace=src",
    "deploy:functions": "npm run deploy --workspace=functions"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
